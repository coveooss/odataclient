export enum ODataQueryOption {
    Count = "$count",
    Expand = "$expand",
    Filter = "$filter",
    OrderBy = "$orderby",
    Select = "$select",
    Top = "$top"
}

export const CRLF = "\r\n";
